# PR: Hyperparameter search job

Context: 500 configurations are evaluated on a small fraud dataset. The winning configuration will be promoted automatically if validation AUC exceeds 0.92. Search runs on a shared four-GPU machine.